const INITIAL_STATE = {
  startDate: null,
  endDate: null,
};

let rootReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default rootReducer;
